// Gamepad — STUB (Ronda 2: gamepad input removed)
// Original: 440 lines → inert stub

window.MMD_SA_createGamepad = function () {
    // Original had no return statement — MMD_SA.Gamepad is undefined.
    // Gamepad menu code only runs on user interaction, so this is safe.
};
